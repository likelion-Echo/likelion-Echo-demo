# Echo 실행 방법

## 매번 하는 것 (터미널 3개)

```bash
docker compose up -d
```

```bash
cd backend && .venv\Scripts\uvicorn app.main:app --host 0.0.0.0 --reload
```

```bash
cd frontend && npm run dev -- -H 0.0.0.0
```

- 화면 http://localhost:3000
- API 문서 http://localhost:8000/docs

`--host 0.0.0.0` / `-H 0.0.0.0`은 다른 기기에서 붙을 때만 필요하지만, 붙여둬도 로컬 사용에 지장이 없어서 그냥 항상 이렇게 띄우는 게 편하다.

---

## 처음 1회만

### 0. 설치

Docker Desktop(고래 아이콘이 초록색 "Engine running"이 될 때까지 대기), Python 3.10+, Node.js 18+, OpenAI API 키.

### 1. 백엔드 세팅

```bash
cd backend
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt
copy .env.example .env
```

`.env`에서 두 값을 채운다.

| 키 | 설명 |
|---|---|
| `OPENAI_API_KEY` | 없으면 기록 등록·페르소나·채팅이 전부 실패한다 |
| `JWT_SECRET` | 아무 랜덤 문자열. **비어 있으면 서버가 아예 안 뜬다** |

### 2. 프론트엔드 세팅

```bash
cd frontend
npm install
```

### 3. DB 테이블 만들기

```bash
cd backend && .venv\Scripts\python scripts/reset_db.py
```

> `reset_db.py`는 **기존 테이블을 전부 지운다.** 스키마가 바뀌었을 때만 다시 돌린다.

### 4. 데모 데이터 넣기

```bash
cd backend && .venv\Scripts\python scripts/seed_demo.py
```

OpenAI 임베딩·페르소나 생성을 실제로 호출하므로 20~30초 걸린다.

| 역할 | 이메일 | 비밀번호 |
|---|---|---|
| 작성자 (아빠) | `dad@echo.demo` | `echo1234` |
| 수신자 (자녀) | `child@echo.demo` | `echo1234` |

"취업 성공" 이벤트가 수신자에게 연결까지 끝난 상태로 만들어진다. 데모 중에 DB를 망가뜨려도 이 명령 하나로 30초면 복구된다.

---

## 같은 와이파이의 다른 기기에서 접속하기

**`.env` 파일은 건드리지 않아도 된다.** 프론트엔드가 접속한 주소에서 호스트를 그대로 빌려 쓰고(`lib/api.js`), 백엔드도 사설망 대역을 자동으로 허용한다(`CORS_ALLOW_LAN`). IP가 바뀌어도 그대로 동작한다.

### 1. 내 IP 확인

```bash
ipconfig
```

Wi-Fi 어댑터의 IPv4 주소를 쓴다 (예: `192.168.0.71`).

### 2. 방화벽 열기 (최초 1회, **관리자 권한** PowerShell)

```powershell
New-NetFirewallRule -DisplayName "Echo demo" -Direction Inbound -Protocol TCP -LocalPort 3000,8000 -Action Allow -Profile Public,Private
```

### 3. 두 서버를 `0.0.0.0`으로 띄운다

맨 위 "매번 하는 것"의 명령 그대로면 이미 되어 있다.

### 4. 친구는 `http://192.168.0.71:3000` 으로 접속

> 카페·학교 와이파이는 기기 간 통신(AP isolation)을 막는 경우가 많다. 다 했는데 안 되면 핸드폰 핫스팟에 둘 다 붙는 게 빠르다.

데모가 끝나면 방화벽 규칙을 지운다.

```powershell
Remove-NetFirewallRule -DisplayName "Echo demo"
```

---

## 자주 나는 문제

| 증상 | 해결 |
|---|---|
| 백엔드가 `JWT_SECRET ...` 에러로 안 뜸 | `.env`의 `JWT_SECRET`을 채운다 |
| `reset_db.py`가 connection refused | DB가 안 떠 있다. `docker compose ps`로 `Up` 확인 |
| `503 OPENAI_API_KEY ...` | `.env` 확인 후 백엔드 재시작 |
| 채팅에서 관련 기록을 못 찾음 | `MAX_RELEVANT_DISTANCE`를 올린다 (0.70 → 0.85) |
| 엉뚱한 질문에도 기록이 붙음 | `MAX_RELEVANT_DISTANCE`를 내린다 (0.60) |
| 친구 접속 시 화면은 뜨는데 로그인 안 됨 | 서버를 `0.0.0.0`으로 띄웠는지, 방화벽 규칙이 있는지 확인 |
| 프론트 빌드가 `a[d] is not a function` | 캐시 문제다. `rm -rf .next` 후 다시 빌드 |
| 글꼴이 평소와 다르게 보임 | 웹폰트를 CDN에서 받는다. 인터넷이 끊기면 시스템 글꼴로 대체된다 |

## DB 직접 보기

```bash
docker compose exec db psql -U echo -d echo
```

`\dt` 테이블 목록, `\d 테이블명` 구조, `\q` 나가기.
