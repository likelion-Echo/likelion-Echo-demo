# Echo 실행 방법

로컬에서 Echo를 처음부터 띄우는 순서. 팀원 각자 이 순서대로 하면 된다.

## 0. 사전 준비 (최초 1회)

- **Docker Desktop** 설치 후 실행 (고래 아이콘이 "Engine running" 초록색이 될 때까지 대기)
- **Python 3.10+**, **Node.js 18+**
- **OpenAI API 키** — 기록 임베딩·페르소나·채팅에 필요하다

## 1. DB 띄우기

프로젝트 루트에서:

```bash
docker compose up -d
```

`pgvector/pgvector:pg16` 컨테이너가 5433 포트로 뜬다. (호스트에 이미 5432를 쓰는 postgres가 있어 5433으로 우회한다.)

확인:

```bash
docker compose ps
```

`likelion-echo-demo-db-1` 이 `Up` 이면 정상.

## 2. 백엔드 세팅

```bash
cd backend
python -m venv .venv
.venv\Scripts\pip install -r requirements.txt
copy .env.example .env
```

`.env`를 열어 **두 값을 반드시 채운다.**

| 키 | 설명 |
|---|---|
| `OPENAI_API_KEY` | 본인 키. 없으면 기록 등록·페르소나·채팅이 전부 실패한다. |
| `JWT_SECRET` | 아무 랜덤 문자열. **비어 있으면 서버가 아예 뜨지 않는다.** |

## 3. DB 테이블 생성

스키마가 코드에 정의돼 있다. 처음이거나 스키마가 바뀐 뒤에는 이걸 돌린다.

```bash
.venv\Scripts\python scripts/reset_db.py
```

> 주의: `reset_db.py`는 **기존 테이블을 전부 지운다.** 데이터를 보존해야 하면 돌리지 말 것.

## 4. 데모 데이터 넣기 (권장)

시연용 계정·기록·페르소나·이벤트를 한 번에 만든다. 실제로 OpenAI 임베딩과 페르소나 생성을 호출하므로 20~30초 걸린다.

```bash
.venv\Scripts\python scripts/seed_demo.py
```

만들어지는 계정:

| 역할 | 이메일 | 비밀번호 |
|---|---|---|
| 작성자 (아빠) | `dad@echo.demo` | `echo1234` |
| 수신자 (자녀) | `child@echo.demo` | `echo1234` |

"취업 성공" 이벤트가 수신자에게 연결까지 끝난 상태로 생성된다.

## 5. 백엔드 실행

```bash
.venv\Scripts\uvicorn app.main:app --reload
```

→ API 문서: http://localhost:8000/docs

## 6. 프론트엔드 실행 (새 터미널)

```bash
cd frontend
npm install
npm run dev
```

→ http://localhost:3000

---

## 같은 와이파이의 다른 기기에서 접속하기

1. 내 PC의 Wi-Fi IPv4 확인:

   ```bash
   ipconfig
   ```

   Wi-Fi 어댑터의 IPv4 주소를 쓴다 (예: `192.168.0.71`). **공유기에서 바뀔 수 있으니 매번 확인.**

2. `frontend/.env.local` 생성 (내 IP로):

   ```
   NEXT_PUBLIC_API_BASE=http://192.168.0.71:8000
   ```

3. `backend/.env`의 `CORS_ORIGINS`에 내 IP 추가:

   ```
   CORS_ORIGINS=http://localhost:3000,http://192.168.0.71:3000
   ```

4. 두 서버를 외부에 열어서 실행:

   ```bash
   .venv\Scripts\uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
   npm run dev -- -H 0.0.0.0
   ```

5. 방화벽 허용 (**관리자 권한** PowerShell):

   ```powershell
   New-NetFirewallRule -DisplayName "Echo demo" -Direction Inbound -Protocol TCP -LocalPort 3000,8000 -Action Allow -Profile Public,Private
   ```

6. 친구는 `http://192.168.0.71:3000` 으로 접속.

> 카페·학교 와이파이는 기기 간 통신(AP isolation)을 막는 경우가 많다. 다 했는데 안 되면 핸드폰 핫스팟에 둘 다 붙는 게 빠르다.
> 데모가 끝나면 방화벽 규칙을 지운다: `Remove-NetFirewallRule -DisplayName "Echo demo"`

---

## 자주 나는 문제

| 증상 | 원인 / 해결 |
|---|---|
| 백엔드가 `JWT_SECRET ...` 에러로 안 뜸 | `.env`의 `JWT_SECRET`을 채운다 |
| `reset_db.py`가 connection refused | DB 컨테이너가 안 떠 있다. `docker compose ps` 확인 |
| `503 OPENAI_API_KEY ...` | `.env`의 `OPENAI_API_KEY` 확인 후 백엔드 재시작 |
| 채팅에서 관련 기록을 못 찾음 | `.env`의 `MAX_RELEVANT_DISTANCE`를 올린다 (기본 0.70 → 0.85) |
| 엉뚱한 질문에도 기록이 붙음 | `MAX_RELEVANT_DISTANCE`를 내린다 (0.60) |
| 친구 접속 시 화면은 뜨는데 로그인 안 됨 | `.env.local`의 IP, `CORS_ORIGINS`, 서버 `--host 0.0.0.0` 재시작 여부 확인 |

## DB 직접 확인

```bash
docker compose exec db psql -U echo -d echo
```

`\dt` 테이블 목록, `\d 테이블명` 구조, `\q` 나가기.
