# Echo 테스트 가이드

데모 전에 핵심 기능이 실제로 동작하는지 확인하는 절차. 특히 이번에 보강한 **AI 안전 로직**을 눈으로 확인하는 데 초점을 둔다.

전제: [실행방법.md](실행방법.md)의 1~6단계를 마치고 `seed_demo.py`까지 실행한 상태.

---

## A. 데모 시나리오 (전체 흐름)

`child@echo.demo` / `echo1234` (자녀 계정)으로 로그인한다.

1. **수신함**에 "취업 성공" 이벤트가 보인다 → 클릭해서 활성화
2. 아빠(이철수)가 남긴 편지·음성이 보인다
3. **Echo와 대화하기** → `아빠 나 오늘 취업했어요` 입력
4. 답변이 나오고, 그 아래 **근거가 된 기록**이 표시된다

이 흐름이 처음부터 끝까지 끊기지 않으면 성공.

---

## B. AI 안전 로직 검증 (이번 수정의 핵심)

### B-1. 검색 임계값 게이트 — 관련 없는 질문

대화창에 기록과 **전혀 무관한** 질문을 넣는다:

```
오늘 서울 날씨 어때?
```

**기대 결과:**
- 답변: `이 내용에 대해서는 남겨진 기록이 없습니다.`
- 근거 기록이 **하나도** 표시되지 않는다
- "남겨진 기록에서 근거를 찾지 못했습니다" 안내가 뜬다

> 왜 중요한가: 예전 코드는 어떤 질문에도 무조건 기록 4개를 근거로 붙였다. 이제 관련 기록이 없으면 LLM을 호출조차 하지 않는다.

### B-2. 인용 강제 — 관련 있는 질문

```
아빠 나 오늘 취업했어요
```

**기대 결과:**
- 취업/첫 직장 관련 편지가 답변의 근거로 표시된다
- 시드에 편지가 5개나 있지만, 근거로는 **실제로 인용한 1~2개만** 뜬다 (5개 전부가 아니다)

### B-3. role 위조 차단

`/docs` (http://localhost:8000/docs) 에서 `POST /chat`을 직접 호출한다. 요청 바디에 `history` 같은 필드를 넣어도 **무시된다** (스키마에 없음). 대화 이력은 서버 DB에서만 가져온다.

### B-4. 대화 저장

대화를 2~3번 주고받은 뒤 **브라우저 새로고침(F5)**.

**기대 결과:** 이전 대화가 그대로 남아 있다.

---

## C. 접근 제어 검증

### C-1. 수신자 분리

- 자녀 계정에는 **자기 기록이 하나도 없다** (나의 기록 = 비어 있음)
- 그런데도 채팅에서는 **아빠의 기록**을 근거로 답변이 나온다
- 즉 "자녀가 아빠의 Echo와 대화"하는 구조가 실제로 동작한다

### C-2. 활성화 전 차단

새 이벤트를 만들고 초대만 한 상태(활성화 전)에서 수신자가 채팅을 시도하면 `403 아직 활성화되지 않은 이벤트입니다` 가 나와야 한다.

### C-3. 초대 코드

- **작성자** 계정으로 미래 메시지 → 이벤트 카드에 **초대 코드**가 보인다
- 다른 계정으로 로그인 → 수신함에 그 코드 입력 → 이벤트가 수신함에 나타난다
- 이미 다른 수신자가 연결된 코드를 또 입력하면 `409` 거부

---

## D. 음성 기록 (STT)

1. 작성자 계정 → 나의 기록 → 새로운 기록 → **🎤 음성** 선택 → 음성 파일 업로드
2. 저장 직후 목록에 "음성을 텍스트로 변환하는 중입니다..." 표시 (업로드 응답은 즉시 온다)
3. 몇 초 뒤 새로고침하면 변환된 텍스트가 채워진다
4. 기록을 펼치면 음성 **재생 플레이어**가 보인다

> 변환이 끝나야 그 음성이 AI 답변의 근거로 검색된다.

---

## E. DB로 결과 확인

채팅이 실제로 저장되고 인용이 제대로 들어갔는지 SQL로 검증:

```bash
docker compose exec db psql -U echo -d echo -c "SELECT role, grounded, cited_memory_ids, left(content,40) FROM chat_messages ORDER BY message_id;"
```

- `role`: user / assistant 가 번갈아 나온다
- `grounded`: 관련 기록을 찾은 답변은 `t`, B-1 같은 경우는 `f`
- `cited_memory_ids`: 실제 인용한 memory_id 배열. `grounded=f`면 비어 있어야 한다

기록 벡터가 잘 쌓였는지:

```bash
docker compose exec db psql -U echo -d echo -c "SELECT m.title, count(c.chunk_id) chunks FROM memories m LEFT JOIN memory_chunks c ON c.memory_id=m.memory_id GROUP BY m.title;"
```

---

## 체크리스트

- [ ] A. 데모 전체 흐름이 끊기지 않는다
- [ ] B-1. 무관한 질문 → "남겨진 기록이 없습니다" + 근거 0개
- [ ] B-2. 관련 질문 → 인용한 기록만 표시 (전부 아님)
- [ ] B-4. 새로고침해도 대화 유지
- [ ] C-1. 자녀 계정이 아빠 기록으로 대화된다
- [ ] C-3. 초대 코드로 수신자 연결된다
- [ ] D. 음성 업로드 → 변환 → 재생
- [ ] E. chat_messages에 cited_memory_ids가 올바르게 저장된다

---

## 임계값 조정

B-2에서 관련 기록이 안 걸리면 `backend/.env`의 `MAX_RELEVANT_DISTANCE`를 올리고(0.70 → 0.85), B-1에서 엉뚱한 기록이 붙으면 내린다(0.60). 바꾼 뒤 백엔드 재시작. **이 값은 실제 데이터로 한 번 맞춰야 한다.**
